server:
  port: 8762
management:
  endpoints:
    web:
      exposure:
        include: health, info
spring:
  application:
    name: api-gateway-service
  profiles:
    active: local
  cloud:
    config:
      fail-fast: true
    devtools:
      livereload:
        port: 0